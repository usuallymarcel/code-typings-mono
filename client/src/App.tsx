// import { useState } from 'react'
import Typing from './components/typing'

function App() {
  // const [count, setCount] = useState(0)

  return (
    <>
      {/* <h1 className="text-3xl font-bold underline">code typings</h1>
      <div></div>
      <button type="button" 
      onClick={() => { 
        setCount(count + 1)
        }}>piss
      </button>
      <p>{count}</p> */}
      <Typing/>
    </>
  )
}

export default App
